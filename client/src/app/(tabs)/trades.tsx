import { ScreenPlaceholder } from '@/components/screen-placeholder';

export default function TradesTab() {
  return <ScreenPlaceholder title="Cambios" subtitle="Repes · coincidencias · ofertas" />;
}
